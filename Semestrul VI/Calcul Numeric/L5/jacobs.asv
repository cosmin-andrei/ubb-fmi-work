function PB1 = jacobs(A,b)
    
    [m,n]=size(A);
    x0 = zeros(size(b)); %val init vect solutie


    M = diag(diag(A));
    N = M - A;
    T = inv(M)*N; 
    c = inv(M)*b; 
    alfa = norm(T,inf);
    x=x0(:);

    M

    while()